# household-site-scrapper
Python scrapping script to populate shopify store

Before running the scrapper install rabbitMQ on server and set the rabbitMQ host and user and allow user access to that host 
